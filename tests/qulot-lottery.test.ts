import {
  assert,
  describe,
  test,
  clearStore,
  beforeAll,
  afterAll
} from "matchstick-as/assembly/index"
import { Address, BigInt } from "@graphprotocol/graph-ts"
import { AdminTokenRecovery } from "../generated/schema"
import { AdminTokenRecovery as AdminTokenRecoveryEvent } from "../generated/QulotLottery/QulotLottery"
import { handleAdminTokenRecovery } from "../src/qulot-lottery"
import { createAdminTokenRecoveryEvent } from "./qulot-lottery-utils"

// Tests structure (matchstick-as >=0.5.0)
// https://thegraph.com/docs/en/developer/matchstick/#tests-structure-0-5-0

describe("Describe entity assertions", () => {
  beforeAll(() => {
    let token = Address.fromString("0x0000000000000000000000000000000000000001")
    let amount = BigInt.fromI32(234)
    let newAdminTokenRecoveryEvent = createAdminTokenRecoveryEvent(
      token,
      amount
    )
    handleAdminTokenRecovery(newAdminTokenRecoveryEvent)
  })

  afterAll(() => {
    clearStore()
  })

  // For more test scenarios, see:
  // https://thegraph.com/docs/en/developer/matchstick/#write-a-unit-test

  test("AdminTokenRecovery created and stored", () => {
    assert.entityCount("AdminTokenRecovery", 1)

    // 0xa16081f360e3847006db660bae1c6d1b2e17ec2a is the default address used in newMockEvent() function
    assert.fieldEquals(
      "AdminTokenRecovery",
      "0xa16081f360e3847006db660bae1c6d1b2e17ec2a-1",
      "token",
      "0x0000000000000000000000000000000000000001"
    )
    assert.fieldEquals(
      "AdminTokenRecovery",
      "0xa16081f360e3847006db660bae1c6d1b2e17ec2a-1",
      "amount",
      "234"
    )

    // More assert options:
    // https://thegraph.com/docs/en/developer/matchstick/#asserts
  })
})
